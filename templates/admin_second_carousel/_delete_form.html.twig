<form method="post" action="{{ path('admin_second_carousel_delete', {'id': second_carousel.id}) }}" onsubmit="return confirm('Es-tu sûre de vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ second_carousel.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
