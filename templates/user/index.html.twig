{% extends 'base.html.twig' %}

{% block title %}Mon Compte{% endblock %}

{% block body %}

{# read and display all flash messages #}
{# {% for label, messages in app.flashes %}
    {% for message in messages %}
    <div class="alert alert-{{ label }}" role="alert">    
            {{ message }}   
    </div>    
    {% endfor %}
{% endfor %} #}


<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="{{path('profile')}}">Mes infos</a>
                </li>
                <li class="nav-item">
                <a class="nav-link" href="{{path('edit-profile')}}">Modifier mon compte</a>
                </li>
                <li class="nav-item">
                <a class="nav-link" href="{{path('favoris')}}">Mes favoris</a>
                </li>
                <li class="nav-item">
                <a class="nav-link" href="{{path('cart')}}">Mon panier</a>
                </li>
                <li class="nav-item">
                <a class="nav-link" href="{{path('mes_commandes')}}">Mes commandes</a>
                </li>
            </ul>
        </div>
    </div>
</nav>


<div class="tab-content" id="myTabContent">
    <div class="tab-pane fade show active" id="home" role="tabpanel" aria-labelledby="home-tab">
        <table class="table table-striped">
            <tr>
                <td>Nom</td>
                <td>{{user.name}}</td>
            </tr>
            <tr>
                <td>Prénom</td>
                <td>{{user.firstname}}</td>
            </tr>
            <tr>
                <td>Email</td>
                <td>{{user.email}}</td>
            </tr>

            <tr>
                <td>Adresse</td>
                {% for adress in user.adresses %}
                <div class="d-flex flex-wrap">
                    <td>{{ adress.name }} - {{ adress.address }} <br> {{adress.zipcode}} {{adress.city}} </td>
                </div>


                {% endfor %}
            </tr>



            
        </table>
        
                    <a href="{{path('edit-profile')}}" class="btn btn-sm bg-success">Modifier</a> 
</div>

{% endblock %}
