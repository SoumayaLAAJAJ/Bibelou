{% extends 'base.html.twig' %}

{% block title %}Echec de la commande{% endblock %}

{% block body %}
    <h3>Echec de la commande</h3>
    <p>Bonjour {{ order.user.firstname }} {{ order.user.name }}</p>
    <p>Votre demande de paiement a échoué. </p>
    <p>Il semblerait que le paiement ait été refusée par votre établissement bancaire.</p>
    <div>
        <a href="{{ path('order') }}">Réessayer le paiement</a>
    </div>
        <a href="{{path('home')}}">Retour à la page d'accueil</a>
{% endblock %}
