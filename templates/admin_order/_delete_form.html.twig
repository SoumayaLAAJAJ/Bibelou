<form method="post" action="{{ path('admin_order_delete', {'id': order.id}) }}" onsubmit="return confirm('Es-tu sûre de vouloir supprimer cette entrée');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ order.id) }}">
    <button class="btn btn-danger mt-2">Supprimer</button>
</form>
